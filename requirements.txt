# Runtime dependencies. Everything is open-source and runs offline:
# inference is served locally by Ollama, embeddings by sentence-transformers.

# --- agent ---
langgraph>=0.2.50
langchain-core>=0.3.20
langchain-ollama>=0.2.0

# --- security: SQL parsing and validation (layer L4) ---
sqlglot>=25.30

# --- data and analysis ---
pandas>=2.2
numpy>=1.26

# --- retrieval over the free-text notes column ---
sentence-transformers>=3.3
faiss-cpu>=1.9

# --- UI ---
# The React front end is served by FastAPI in production and proxied by Vite in
# development. Streamlit is kept as a second, dependency-light interface over
# the same tools -- useful as a fallback when a demo has to work no matter what.
fastapi>=0.115
uvicorn[standard]>=0.32
itsdangerous>=2.2
streamlit>=1.40
plotly>=5.24

# --- auth and audit ---
argon2-cffi>=23.1
structlog>=24.4
pydantic>=2.9
